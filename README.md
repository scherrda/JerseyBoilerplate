# JerseyBoilerplate
Rest Jersey boilerplate : 
a Rest Resource returning JSON data, a list of entities, a unit Test, an integration test

stack : Java7, JSON, Jersey, Jackson, maven3, JUnit, AssertJ

- mvn clean install
- mvn tomcat7:run
